# Secrets & credentials — never commit these
.env
.env.*
*.pem
*.key
*.p12
*.pfx
secrets.*
credentials.*
config.local.*

# Dependencies & build output
node_modules/
dist/
build/

# OS / editor noise
.DS_Store
Thumbs.db
*.log
